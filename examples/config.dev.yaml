server:
  listen: ":18080"
  dataDir: "data/dev"
  repoCacheDir: "data/dev/repos"
auth:
  # Development only. Do not expose this config outside a trusted local shell.
  mode: dev-no-auth
monitoring:
  defaultInterval: 30s
repositories: []
runtime:
  docker: []
  kubernetes: []
  http: []
  ping: []
